import { FC } from "react";
export interface PageProps {
    parentElement: any;
    prev: FC | null;
    current: FC;
    next: FC | null;
    index: number;
    setActive: Function;
    gone?: boolean;
    theme?: string;
}
export default function Page({ prev, current, next, theme, index, setActive, gone, parentElement, }: PageProps): JSX.Element;
