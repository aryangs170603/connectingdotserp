/// <reference types="react" />
interface Props {
    type?: string;
    username: string;
    title: string;
    desc: string;
    image: string;
}
export declare const Twitter: ({ type, username, title, desc, image, }: Props) => JSX.Element;
export {};
