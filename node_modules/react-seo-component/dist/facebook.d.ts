/// <reference types="react" />
interface Props {
    url: string;
    type: string;
    title: string;
    desc: string;
    image: string;
    locale: string;
}
export declare const Facebook: ({ url, type, title, desc, image, locale, }: Props) => JSX.Element;
export {};
