/// <reference types="react" />
interface Props {
    title: string;
    titleTemplate: string;
    titleSeparator?: string;
    description: string;
    pathname: string;
    article?: boolean;
    image?: string;
    siteLanguage: string;
    siteLocale: string;
    twitterUsername: string;
    author?: string;
    datePublished?: string;
    dateModified?: string;
}
export declare const SEO: ({ title, titleTemplate, titleSeparator, description, pathname, article, image, siteLanguage, siteLocale, twitterUsername, author, datePublished, dateModified, }: Props) => JSX.Element;
export {};
